<?xml version="1.0" encoding="UTF-8"?>
<configuration>

	<include resource="org/springframework/boot/logging/logback/defaults.xml" />
	<property name="LOG_FILE"
		value="${LOG_FILE:-${LOG_PATH:-${LOG_TEMP:-${java.io.tmpdir:-/tmp}}/}spring.log}" />
	<include resource="org/springframework/boot/logging/logback/console-appender.xml" />

	<springProfile name="default">

		<!-- debug security package -->
		<!-- <logger name="org.springframework.security" level="DEBUG" /> -->
		<!-- hibernate show sql -->
		<!--<logger name="org.hibernate.SQL" level="DEBUG" />-->
		<!--&lt;!&ndash;  <logger name="org.hibernate.type" level="TRACE" />  &ndash;&gt;-->
		<!--<root level="INFO">-->
			<!--<appender-ref ref="CONSOLE" />-->
		<!--</root>-->

		<appender name="APPENDER" class="ch.qos.logback.core.rolling.RollingFileAppender">
			<File>logs/rest-api.log</File>
			<encoder>
				<pattern>%X{rid} %d{yyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %c{1.} - %msg%n</pattern>
			</encoder>
			<rollingPolicy class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
				<maxIndex>10</maxIndex>
				<FileNamePattern>logs/rest-api.log.%i</FileNamePattern>
			</rollingPolicy>
			<triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
				<MaxFileSize>10000KB</MaxFileSize>
			</triggeringPolicy>
		</appender>

		<appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
			<withJansi>true</withJansi>
			<encoder>
				<pattern>%X{rid} %d{yyy-MM-dd HH:mm:ss.SSS} %green([%thread]) %highlight(%-5level) %cyan(%c{1.}) - %msg%n</pattern>
			</encoder>
		</appender>

		<root level="INFO">
			<appender-ref ref="APPENDER" />
			<appender-ref ref="CONSOLE" />
		</root>

	</springProfile>

	<springProfile name="production">
	    <!--
		<appender name="ROLLING-FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
			<encoder>
				<pattern>${FILE_LOG_PATTERN}</pattern>
			</encoder>
			<file>${LOG_FILE}</file>
			<rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
				<fileNamePattern>${LOG_FILE}.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
				<maxFileSize>10MB</maxFileSize>
				<totalSizeCap>5GB</totalSizeCap>
			</rollingPolicy>
		</appender>
 		-->
		<appender name="FILE_ERROR" class="ch.qos.logback.core.FileAppender">
        	<file>${LOG_FOLDER}/error.log</file>
        	<append>true</append>
			<filter class="ch.qos.logback.classic.filter.ThresholdFilter">
				<level>ERROR</level>
			</filter>
			<encoder>
			    <pattern>%d %-5level [%thread]: %message%n%ex{2}</pattern>
				<!--   <pattern>${FILE_LOG_PATTERN}</pattern> -->
			</encoder>
    	</appender>
		<root level="INFO">
			<appender-ref ref="FILE_ERROR" />
			<appender-ref ref="CONSOLE" />
		</root>
	</springProfile>

</configuration>